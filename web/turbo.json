{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [
        ".svelte-kit/output/**",
        ".svelte-kit/cloudflare/**",
        "build/**"
      ]
    },
    "typecheck": {
      "outputs": [".svelte-kit/**"]
    }
  }
}
